# 🎲 Рандомайзер текста (Spintax)

**Кастомное активити (действие) для бизнес-процессов и роботов Битрикс24.**  
Позволяет генерировать уникальные тексты на основе заданных переменных с учетом **веса (вероятности выпадения)**, а также поддерживает классический Spintax и многоуровневую вложенность ("матрешку").

Идеально подходит для автоматических рассылок (Email, WhatsApp, Telegram, SMS), создания уникальных описаний задач и обхода спам-фильтров за счет рандомизации типовых сообщений.

---

## ✨ Ключевые возможности

*   📊 **Умное распределение шансов (весов)** 
    Вы можете задать процент вероятности для каждого варианта текста. Скрипт оснащен умной валидацией:
    * *Сумма = 100%* — строго соблюдается.
    * *Пустые значения* — если вы указали вес только для части вариантов, система автоматически и поровну распределит остаток процентов между пустыми полями.
*   🪆 **Многоуровневая вложенность (Матрешка)**
    Переменные можно использовать внутри других переменных (например, `{Var2}` внутри значения для `{Var1}`). Встроена защита от бесконечных циклов (лимит 15 уровней).
*   🔀 **Классический Spintax из коробки**
    Помимо создания переменных, вы можете писать варианты перебора прямо в итоговом тексте: `{Добрый день | Приветствую | Здравствуйте}` (шансы 50/50).
*   🔗 **Полная совместимость с CRM Битрикс24**
    Встроенная кнопка `[...]` позволяет бесшовно вставлять в текст стандартные теги полей документа (например, имя контакта: `{=Document:NAME}`).

---

## 🚀 Установка

*Модуль предназначен только для коробочной версии Битрикс24 (On-Premise), так как требует размещения файлов на сервере.*

1. Перейдите в корневую директорию вашего сайта на сервере.
2. Проверьте наличие директории `/local/activities/custom/`. Если её нет — создайте.
3. В папке `custom` создайте новую папку с названием `textrandomizeractivity`.
4. Разместите внутри неё 3 файла из этого репозитория:
   * `.description.php`
   * `textrandomizeractivity.php`
   * `properties_dialog.php`
5. Перейдите в настройки любого бизнес-процесса в Битрикс24. Активити появится в панели справа в разделе **"CRM"** под названием **"Рандомайзер текста (Spintax)"**.

📂 **Структура файлов должна выглядеть так:**
```text
/bitrix/
  /activities/
    /custom/
      /textrandomizeractivity/
        ├── .description.php
        ├── textrandomizeractivity.php
        └── properties_dialog.php
```

---

## 🛠 Как использовать (Инструкция)

1. Перетащите активити **"Рандомайзер текста (Spintax)"** на схему бизнес-процесса.
2. Откройте настройки действия.
3. В блоке **Настройка переменных** нажмите `+ Добавить переменную`.
4. Задайте имя (например, `Greeting` или `Offer`) и добавьте несколько вариантов текста.
5. Укажите желаемый шанс выпадения в поле `%`. *(Если оставить пустым — скрипт сам рассчитает равные доли).*
6. В поле **Итоговый шаблон текста** напишите ваш финальный текст.
   *Пример:* 
   > `{Привет|Добрый день}, уважаемый {=Document:NAME}! Хотел {Var1} и предложить {Var2}.`
7. Сохраните настройки.
8. В следующем активити (например, "Отправить письмо") вставьте сгенерированный текст через форму **Вставка значения -> Дополнительные результаты -> Рандомайзер текста -> Сгенерированный текст**.

---

## 💡 Примеры сценариев

### Сценарий 1: Разогрев базы (А/Б тестирование офферов)
Вам нужно отправить сообщение клиенту, но вы хотите протестировать, какой призыв к действию работает лучше, при этом большую часть трафика пустить на проверенный вариант.
* `{Var_Offer}`
  * Вариант А: "Скидка 10% до конца недели" — **70%**
  * Вариант Б: "Бесплатная доставка при заказе сегодня" — **30%**

### Сценарий 2: Защита от спама в WhatsApp
Мессенджеры часто блокируют номера за отправку абсолютно одинаковых сообщений. Рандомизация делает каждое сообщение уникальным на уровне байтов и смысла:
**Итоговый шаблон:** 
`{Здравствуйте|Приветствую|Добрый день}! Ваша {заявка|бронь} номер {=Document:ID} успешно {принята|обработана}. {Ожидайте звонка|Скоро с вами свяжемся}.`
